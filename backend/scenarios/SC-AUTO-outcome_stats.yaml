id: SC-AUTO-outcome_stats
title: "Lookup \xB7 outcome stats"
domain: Custom data sources
autonomous: true
actor_id: agent-data-lookup
operator_role:
  label: Operator
  name: data.analyst
action_type: data_lookup
action_payload:
  source_id: outcome_stats
  ontology_type: OutcomeStat
  scope: data.read
match_keywords:
- lookup outcome stats
- outcome
- outcome stats
- outcome_stats
- outcomestat
- query outcome stats
- show outcome stats
- stats
interpreted_as: look up data from outcome stats
clarifying_question: "Just to confirm \u2014 pull data from <code>outcome_stats</code>\
  \ (sqlite). Read-only query within my <code>data.read</code> scope; per policy <code>GR-AUTO-LOOKUP</code>\
  \ no human review is required. Proceed?"
auto_approval_guardrail: GR-AUTO-LOOKUP
auto_approval_reason: Read-only query against operator-registered source 'outcome_stats'.
  Policy GR-AUTO-LOOKUP permits autonomous lookups against trusted operator sources.
closing_message: "Done \u2014 pulled rows from <code>outcome_stats</code>. The result\
  \ is on the audit trail."
stages:
  agent_intake:
    binder: AutoLookupAgentBinder/1.0
    facts:
    - source: kf:graph
      ontology_type: Policy
      id: POL-AUTO-LOOKUP
      uri: kf.tcs/policy/POL-AUTO-LOOKUP
      title: Autonomous data lookup policy
      payload: Read-only queries against operator-registered data sources are autonomous
        within the data.read scope.
    - source: iam:scopes
      ontology_type: ActorScope
      id: agent-data-lookup
      uri: iam.tcs/actors/agent-data-lookup
      title: Actor scope
      payload: "Scopes: data.read \xB7 operator-registered source: outcome_stats"
  proposal:
    binder: AutoLookupProposalBinder/1.0
    queries:
    - data_source: outcome_stats
      ontology_type: OutcomeStat
      purpose: Lookup against operator-registered source outcome_stats
      filter: {}
outcomes:
  auto_execute:
    headline: "Auto-executed \u2014 data retrieved from outcome_stats"
    detail: Lookup against outcome_stats (sqlite) completed. Rows are bound to the
      case envelope and recorded in the audit trail.
_auto_for_source: outcome_stats
